# Keystore 설정 (Google Play 출시 서명용)
# 이 파일은 템플릿 — 실제 사용 시 keystore.properties로 복사 후 값 채우기.
# ⚠️ keystore.properties와 .jks 파일은 절대 Git에 push 금지 (.gitignore 처리됨)

storeFile=upload-keystore.jks
storePassword=YOUR_KEYSTORE_PASSWORD
keyAlias=upload
keyPassword=YOUR_KEY_PASSWORD
